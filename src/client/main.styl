@import './variables.styl'

*
  margin 0
  padding 0

body
  width 100vw
  font-size 100%
  background-color color-white
  font-family 'PT Sans', Arial, sans-serif
  

h1, h2, h3, h4
  margin 0
  font-family 'PT Serif', sans-serif
  font-weight 400
  text-align center
  
button, .button, input, textarea, select
  font-size .8em
  padding .5em 1em
  border-radius 2px
  border 1px solid darken(color-gray, 15)
  text-decoration none
  transition border 0.5s, box-shadow 0.5s
  
  &:focus
    border 1px solid darken(color-gray, 35)
    box-shadow  0 0 3px color-light

button, .button
  background-color lighten(color-lightest, 50)
  color color-black
  cursor pointer
  
  &:hover
    background-color lighten(color-lightest, 40)
    border 1px solid darken(color-gray, 25)

  &.disabled
    background-color lighten(color-gray, 50)
    color lighten(color-black, 40)

input, textarea, select
  color color-normal
  
  &:focus
    border 1px solid color-darkest
    box-shadow  0 0 3px color-light

*:focus
  outline none
  

.Loader, .Loader:before, .Loader:after
  background color-white
  animation load1 1s infinite ease-in-out
  width 1em
  height 4em
  
.Loader:before, .Loader:after
  position absolute
  top 0
  content ''
  
.Loader:before
  left -1.5em
  animation-delay -0.32s
  
.Loader
  color color-white
  text-indent -9999em
  margin 88px auto
  position relative
  font-size 11px
  transform translateZ(0)
  animation-delay -0.16s

.Loader:after
  left 1.5em

@keyframes load1
  0%, 80%, 100%
    box-shadow 0 0
    height 4em
    
  40%
    box-shadow 0 -2em
    height 5em

  
  